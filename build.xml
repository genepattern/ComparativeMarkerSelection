<?xml version="1.0" encoding="UTF-8"?>
<!-- Ant build file -->

<project basedir="" default="package" name="ComparativeMarkerSelection">
	<property name="dirOffset" value="../"/>
	<import file="${dirOffset}/commontargets.xml"/>
	
	<target name="_init" depends="init">
		<property name="appfile" value="MarkerSelection.jar"/>
		<property name="inclds" value="edu/"/>

		<path id="library.classpath">
			<fileset dir="${libpath}" id="common.lib" includes="Jama-1.0.1.jar,colt.jar, 
			gp-modules.jar, trove.jar, broad-cg.jar, commons-math-1.1.jar" />
			<fileset dir="lib" id="module.lib" includes="jsci-core.jar" />
		</path>
	</target>
		
	
	<target name="package" depends="_init, clean, compile">
		<jar basedir="." defaultexcludes="true" excludes="**/*.java, **/*.form, **/.nbattrs" includes="${inclds}" jarfile="${appfile}"></jar>
	</target>
	
	<target name="clean">
		<delete>
			<fileset dir="." includes="**/*.class"/>
		</delete>
	</target>
	
	<target name="compile" depends="_init">
		<javac debug="true" defaultexcludes="true" deprecation="true" destdir="." includes="${inclds}" optimize="false" proceed="false" srcdir="src" target="${java.target}" source="${java.target}">
			<classpath refid="library.classpath"/>
		</javac>
	</target>

	<target depends="clean,compile" name="build"/>
	
	<target depends="_init, package" name="create-zip">
		<antcall target="prezip"/>
		
		<zip destfile="${dest.dir}/${ant.project.name}.zip" whenempty="fail" defaultexcludes="true">
			<fileset dir="." includes="manifest, ${appfile}, qvalue.R, *.pdf"/>
			<fileset refid="common.lib"/>
			<fileset refid="module.lib"/>
		</zip>
		<antcall target="postzip"/>
	</target>

</project>
